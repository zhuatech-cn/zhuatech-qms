spring:
  datasource:
    url: jdbc:h2:mem:qms;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
app:
  jwt:
    secret: test-zhuatech-qms-secret-at-least-32-bytes-long
    expiration: PT1H
  cors:
    allowed-origins: http://localhost:5173,http://127.0.0.1:5173
